name: Deploy Lambda Scheduler

on:
  push:
    branches:
      - main
    paths:
      - 'lambdas/scheduler/**'
      - '.github/workflows/deploy-scheduler.yml'

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: aws
    defaults:
      run:
        working-directory: ./lambdas/scheduler

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "${{ secrets.AWS_ROLE_ARN }}"
          aws-region: "${{ secrets.AWS_REGION }}"

      - name: Zip Lambda
        run: zip -r lambda.zip index.mjs

      - name: Update Lambda Code
        run: aws lambda update-function-code --function-name ${{ secrets.SCHEDULER_FUNCTION_NAME }} --zip-file fileb://lambda.zip
